import React from "react";
import BaseTypeChip from "./BaseTypeChip";

const RockChip = () => {
    return (
        <BaseTypeChip label={'Rock'} bgColour={'#AFA981'} textColour={'white'}/>
    );
};

export default RockChip;